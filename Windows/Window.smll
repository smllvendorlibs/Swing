import Swing::Menus::MenuBar

type JFrame = "JFrame"

fn window_new(title: String): JFrame => {
  val frame = null of JFrame
  java {
    "frame = new JFrame(title);"
  }
  frame
}

fn window_set_size(window: JFrame, width: Int, height: Int): JFrame => {
  java {
    "window.setSize(width, height);"
  }
  window
}

fn window_set_visible(window: JFrame, toggle: Bool): JFrame => {
  java {
    "window.setVisible(toggle);"
  }
  window
}

fn window_add(window: JFrame, child: Any): JFrame => {
  java {
    "window.add((Component) child);"
  }
  window
}

fn window_pack(window: JFrame): JFrame => {
  java {
    "window.pack();"
  }
  window
}


